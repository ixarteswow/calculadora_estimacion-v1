// Local: copia como js/config.js y rellena con tu proyecto Supabase.
// Netlify: no uses este archivo; define SUPABASE_URL y SUPABASE_KEY en
// Site settings → Environment variables (el build genera js/config.js).
// js/config.js está en .gitignore — no lo subas a GitHub.

window.CONFIG = {
    SUPABASE_URL: 'https://TU-PROYECTO.supabase.co',
    SUPABASE_KEY: 'TU_CLAVE_ANON_O_PUBLISHABLE'
};
